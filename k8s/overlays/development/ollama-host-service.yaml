apiVersion: v1
kind: Service
metadata:
  name: ollama-host
  namespace: atlas-mcp
spec:
  type: ExternalName
  externalName: host.docker.internal
  ports:
  - port: 11434
    targetPort: 11434
    protocol: TCP
